import Const
# get file path and word index from the user
"""
def get_values_from_user():
    path_to_file = str(input("Please enter path to file: "))
    index_from_user = int(input("Please enter a number thar represent a word index: "))
    return path_to_file, index_from_user
"""

"""
This line is test commit from github
deleted line -----------------------------
"""

"""
def try_update_letter_guessed(letter_guessed, old_letters_guessed):

    if not check_valid_input(letter_guessed.lower(), old_letters_guessed):
        print('X')
        if (len(old_letters_guessed) == 1):
            #print("Your oldest guesses are: ")
            print(old_letters_guessed)
        else:
            if (len(old_letters_guessed) > 1):
                for guess in old_letters_guessed[0: -1]:
                    print(guess + " -> ", end="")
                print(old_letters_guessed[-1])
        return False
    else:
        return True
"""

abc = ["a", "b" ,"c" ,"d"]
old_letters_guessed_to_print =

print(old_letters_guessed_to_print)
